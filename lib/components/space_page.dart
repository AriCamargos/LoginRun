import 'package:flutter/material.dart';

class SpacePage80 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 80,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage25 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 25,
    ); //Altura do button username até o o button password.; 
  }
}

class SpacePage40 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 40,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage10 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage65 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 65,
    ); //Altura da imagem até o o botão username.;
  }
}
