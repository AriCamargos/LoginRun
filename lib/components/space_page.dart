import 'package:flutter/material.dart';

class SpacePage80 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 80,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage25 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 25,
    ); //Altura do button username até o o button password.;
  }
}

class SpacePage40 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 40,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage45 extends StatelessWidget {
  const SpacePage45({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 45,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage10 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage15 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 15,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePage65 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 65,
    ); //Altura da imagem até o o botão username.;
  }
}

class SpacePageWidth5 extends StatelessWidget {
  const SpacePageWidth5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 8,
    );
  }
}

class SpacePageWidth103 extends StatelessWidget {
  const SpacePageWidth103({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 103,
    );
  }
}
